#
# luci-proto-atc — LuCI support for the ATC (AT-commands) protocol
# Source: mrhaav/openwrt (vendored locally to avoid mrhaav feed conflicts)
#
include $(TOPDIR)/rules.mk

LUCI_TITLE:=Support for AT-commands script, atc
LUCI_DEPENDS:=
LUCI_PKGARCH:=all

PKG_VERSION:=2025.01.10
PKG_RELEASE:=2
PKG_MAINTAINER:=Henrik Ginstmark <henrik@ginstmark.se>

include ../../luci.mk

# call BuildPackage - OpenWrt buildroot signature
